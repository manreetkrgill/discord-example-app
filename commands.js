// This file is for registering slash commands with Discord
// However, the current implementation uses automatic message detection
// and interactive modals, so slash command registration is not needed.
// The bot listens to all messages and automatically detects sensitive information.

console.log('Slash command registration not used - bot uses automatic message detection');
